# 2022 experiment files

These are the original scripts from this repository’s first commit
(20 Feb 2022), while the Sensors paper was being written.

They are messy research notes (2D/3D U-Net variants, skip-connection
trials, a few NIfTI volumes and checkpoints). The maintained
implementation is in `src/attn3dunet/` at the repo root.
